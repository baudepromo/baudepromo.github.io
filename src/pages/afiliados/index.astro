<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Programa de Afiliados | Baú de Promoções</title>
  <link rel="icon" href="/favicon.webp"/>


  <style is:inline>
    /* --- NOVA IDENTIDADE VISUAL --- */
    :root {
      --primary-gradient: linear-gradient(135deg, #ff4b4b 0%, #ff9000 100%);
      --bg-body: #f5f5f5;
      --text-dark: #1a1a1a;
      --text-muted: #666666;
      --white: #ffffff;
      --card-shadow: 0 4px 20px rgba(0,0,0,0.08);
      --accent-color: #ff4b4b;
    }

    body {
      background-color: var(--bg-body);
      color: var(--text-dark);
      font-family: 'Inter', sans-serif;
      height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      overflow: hidden;
    }

    /* --- CARD CENTRAL --- */
    .construction-container {
      background: var(--white);
      padding: 50px 40px;
      max-width: 550px;
      width: 90%;
      border-radius: 24px;
      box-shadow: var(--card-shadow);
      border: 1px solid rgba(0,0,0,0.03);
    }

    /* --- ANIMAÇÃO ÍCONE --- */
    .icon-box {
      font-size: 80px;
      margin-bottom: 20px;
      display: inline-block;
      animation: pulse 2s infinite ease-in-out;
      /* Removemos filtros antigos, o emoji nativo funciona bem, 
         ou podemos aplicar sombra colorida */
    }

    @keyframes pulse {
      0% { transform: scale(1); filter: drop-shadow(0 0 0px rgba(255, 75, 75, 0)); }
      50% { transform: scale(1.1); filter: drop-shadow(0 0 20px rgba(255, 75, 75, 0.4)); }
      100% { transform: scale(1); filter: drop-shadow(0 0 0px rgba(255, 75, 75, 0)); }
    }

    /* --- BADGE --- */
    .badge {
      background: rgba(255, 75, 75, 0.1);
      color: var(--accent-color);
      padding: 6px 16px;
      border-radius: 50px;
      font-size: 0.85rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      display: inline-block;
      margin-bottom: 15px;
    }

    /* --- TIPOGRAFIA --- */
    h1 {
      font-size: 2.2rem;
      font-weight: 700;
      margin-bottom: 15px;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      color: var(--accent-color); /* Fallback */
    }

    p {
      color: var(--text-muted);
      font-size: 1.1rem;
      margin: 15px 0 30px;
      line-height: 1.5;
    }

    /* --- BARRA DE PROGRESSO --- */
    .loader-bar {
      width: 100%;
      height: 8px;
      background: #e0e0e0; /* Fundo cinza claro */
      border-radius: 10px;
      margin: 30px 0 10px; /* Margem ajustada */
      overflow: hidden;
      position: relative;
    }

    .loader-progress {
      position: absolute;
      width: 60%; /* Progresso manual */
      height: 100%;
      background: var(--primary-gradient);
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(255, 75, 75, 0.4);
    }

    /* --- BOTÃO VOLTAR --- */
    .btn-back {
      display: inline-block;
      background: var(--primary-gradient);
      color: var(--white);
      text-decoration: none;
      font-weight: 700;
      padding: 14px 30px;
      border-radius: 50px;
      transition: 0.3s;
      margin-top: 20px;
      box-shadow: 0 5px 15px rgba(255, 75, 75, 0.2);
    }

    .btn-back:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(255, 75, 75, 0.4);
    }
  </style>
</head>
<body>

  <div class="construction-container">
    <div class="icon-box">🤝</div>
    <br>
    <span class="badge">Em Desenvolvimento</span>
    <h1>Programa de Afiliados</h1>
    <p>Estamos preparando uma plataforma incrível para você lucrar indicando os melhores achadinhos da internet. Em breve, você poderá fazer parte do time do Baú!</p>
    
    <div class="loader-bar">
      <div class="loader-progress"></div>
    </div>
    
    <p style="font-size: 0.9rem; color: #999;">Previsão de lançamento: Julho</p>
    
    <a href="/" class="btn-back">← Voltar ao Início</a>
  </div>

</body>
</html>
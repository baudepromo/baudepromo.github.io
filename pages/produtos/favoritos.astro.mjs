import { c as createComponent, r as renderTemplate, b as renderComponent, m as maybeRenderHead } from '../../chunks/astro/server_B1wTNOfG.mjs';
import 'piccolore';
import 'html-escaper';
import { $ as $$MainLayout, a as $$Global } from '../../chunks/MainLayout_Cxp42-Ip.mjs';
import 'clsx';
export { renderers } from '../../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Favoritos = createComponent(async ($$result, $$props, $$slots) => {
  return renderTemplate(_a || (_a = __template(['<script type="module">\n    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";\n    import { getAuth, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js";\n    import { getFirestore, doc, getDoc } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";\n\n    const firebaseConfig = {\n        apiKey: "AIzaSyAltYkTpdrIShVv7wJXsszQqvOZXTC45Wg",\n        authDomain: "baudepromocao-oficial.firebaseapp.com",\n        projectId: "baudepromocao-oficial",\n        storageBucket: "baudepromocao-oficial.firebasestorage.app",\n        messagingSenderId: "743146859125",\n        appId: "1:743146859125:web:6d1f5331e22b8ce981263c"\n    };\n\n    const app = initializeApp(firebaseConfig);\n    const auth = getAuth(app);\n    const db = getFirestore(app);\n\n    onAuthStateChanged(auth, async (user) => {\n        if (user) {\n            loadUserFavorites(user.uid);\n        } else {\n            // Usu\xE1rio n\xE3o logado - Visual atualizado\n            document.getElementById("fav-grid").innerHTML = `\n                <div class="empty-state">\n                    <i class="fa-solid fa-lock empty-icon" style="color: #ff4b4b;"></i>\n                    <h2>Acesso Restrito</h2>\n                    <p>Fa\xE7a login para ver sua lista de desejos.</p>\n                    <a href="index.html" class="pill-btn btn-details" style="max-width: 200px; margin: 0 auto;">Ir para Login</a>\n                </div>\n            `;\n        }\n    });\n\n    async function loadUserFavorites(uid) {\n        const grid = document.getElementById("fav-grid");\n        \n        try {\n            // 1. Buscar IDs no Firestore\n            const snap = await getDoc(doc(db, "wishlists", uid));\n            \n            if (!snap.exists() || !snap.data().items || snap.data().items.length === 0) {\n                grid.innerHTML = `\n                    <div class="empty-state">\n                        <i class="fa-regular fa-heart empty-icon"></i>\n                        <h2>Sua lista est\xE1 vazia</h2>\n                        <p>Favorite alguns produtos e eles aparecer\xE3o aqui!</p>\n                        <a href="index.html" class="pill-btn btn-details" style="background: var(--primary-gradient); border:none; max-width: 200px; margin: 0 auto;">Ver Ofertas</a>\n                    </div>\n                `;\n                return;\n            }\n\n            const favoriteIds = snap.data().items.map(id => String(id));\n\n            // 2. Buscar dados no products.json\n            const response = await fetch(\'/data/produtos.json\');\n            const products = await response.json();\n\n            // 3. Filtrar produtos\n            const myFavorites = products.filter(p => favoriteIds.includes(String(p.id)));\n\n            if (myFavorites.length === 0) {\n                grid.innerHTML = `\n                    <div class="empty-state">\n                        <i class="fa-regular fa-face-frown empty-icon"></i>\n                        <h2>Nenhuma oferta ativa.</h2>\n                        <p>Os produtos que voc\xEA salvou podem ter sa\xEDdo de promo\xE7\xE3o.</p>\n                    </div>`;\n                return;\n            }\n\n            // 4. Renderizar (USANDO O NOVO ESTILO VISUAL)\n            grid.innerHTML = "";\n            myFavorites.forEach(p => {\n                const encodedId = btoa(encodeURIComponent(String(p.id).trim()));\n                \n                grid.innerHTML += `\n                    <div class="card">\n                        <img src="${p.img}" alt="${p.title}">\n                        <h3>${p.title}</h3>\n                        <span class="price">R$ ${p.price}</span>\n                        <a href="/produtos/info/index.html?id=${encodedId}" class="pill-btn btn-details">\n                            Ver Oferta <i class="fa-solid fa-arrow-right"></i>\n                        </a>\n                    </div>\n                `;\n            });\n\n        } catch (error) {\n            console.error("Erro:", error);\n            grid.innerHTML = `\n                <div class="empty-state">\n                    <i class="fa-solid fa-triangle-exclamation empty-icon" style="color: #ff9000;"></i>\n                    <p>Ocorreu um erro ao carregar seus favoritos.</p>\n                </div>`;\n        }\n    }\n<\/script>'], ['<script type="module">\n    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";\n    import { getAuth, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js";\n    import { getFirestore, doc, getDoc } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";\n\n    const firebaseConfig = {\n        apiKey: "AIzaSyAltYkTpdrIShVv7wJXsszQqvOZXTC45Wg",\n        authDomain: "baudepromocao-oficial.firebaseapp.com",\n        projectId: "baudepromocao-oficial",\n        storageBucket: "baudepromocao-oficial.firebasestorage.app",\n        messagingSenderId: "743146859125",\n        appId: "1:743146859125:web:6d1f5331e22b8ce981263c"\n    };\n\n    const app = initializeApp(firebaseConfig);\n    const auth = getAuth(app);\n    const db = getFirestore(app);\n\n    onAuthStateChanged(auth, async (user) => {\n        if (user) {\n            loadUserFavorites(user.uid);\n        } else {\n            // Usu\xE1rio n\xE3o logado - Visual atualizado\n            document.getElementById("fav-grid").innerHTML = \\`\n                <div class="empty-state">\n                    <i class="fa-solid fa-lock empty-icon" style="color: #ff4b4b;"></i>\n                    <h2>Acesso Restrito</h2>\n                    <p>Fa\xE7a login para ver sua lista de desejos.</p>\n                    <a href="index.html" class="pill-btn btn-details" style="max-width: 200px; margin: 0 auto;">Ir para Login</a>\n                </div>\n            \\`;\n        }\n    });\n\n    async function loadUserFavorites(uid) {\n        const grid = document.getElementById("fav-grid");\n        \n        try {\n            // 1. Buscar IDs no Firestore\n            const snap = await getDoc(doc(db, "wishlists", uid));\n            \n            if (!snap.exists() || !snap.data().items || snap.data().items.length === 0) {\n                grid.innerHTML = \\`\n                    <div class="empty-state">\n                        <i class="fa-regular fa-heart empty-icon"></i>\n                        <h2>Sua lista est\xE1 vazia</h2>\n                        <p>Favorite alguns produtos e eles aparecer\xE3o aqui!</p>\n                        <a href="index.html" class="pill-btn btn-details" style="background: var(--primary-gradient); border:none; max-width: 200px; margin: 0 auto;">Ver Ofertas</a>\n                    </div>\n                \\`;\n                return;\n            }\n\n            const favoriteIds = snap.data().items.map(id => String(id));\n\n            // 2. Buscar dados no products.json\n            const response = await fetch(\'/data/produtos.json\');\n            const products = await response.json();\n\n            // 3. Filtrar produtos\n            const myFavorites = products.filter(p => favoriteIds.includes(String(p.id)));\n\n            if (myFavorites.length === 0) {\n                grid.innerHTML = \\`\n                    <div class="empty-state">\n                        <i class="fa-regular fa-face-frown empty-icon"></i>\n                        <h2>Nenhuma oferta ativa.</h2>\n                        <p>Os produtos que voc\xEA salvou podem ter sa\xEDdo de promo\xE7\xE3o.</p>\n                    </div>\\`;\n                return;\n            }\n\n            // 4. Renderizar (USANDO O NOVO ESTILO VISUAL)\n            grid.innerHTML = "";\n            myFavorites.forEach(p => {\n                const encodedId = btoa(encodeURIComponent(String(p.id).trim()));\n                \n                grid.innerHTML += \\`\n                    <div class="card">\n                        <img src="\\${p.img}" alt="\\${p.title}">\n                        <h3>\\${p.title}</h3>\n                        <span class="price">R$ \\${p.price}</span>\n                        <a href="/produtos/info/index.html?id=\\${encodedId}" class="pill-btn btn-details">\n                            Ver Oferta <i class="fa-solid fa-arrow-right"></i>\n                        </a>\n                    </div>\n                \\`;\n            });\n\n        } catch (error) {\n            console.error("Erro:", error);\n            grid.innerHTML = \\`\n                <div class="empty-state">\n                    <i class="fa-solid fa-triangle-exclamation empty-icon" style="color: #ff9000;"></i>\n                    <p>Ocorreu um erro ao carregar seus favoritos.</p>\n                </div>\\`;\n        }\n    }\n<\/script>'])));
}, "C:/Users/joaos/OneDrive/Documents/baudepromo/radiant-radiation/src/components/produtos/favoritos.astro", void 0);

const $$Index = createComponent(($$result, $$props, $$slots) => {
  return renderTemplate`${renderComponent($$result, "Layout", $$MainLayout, {}, { "default": ($$result2) => renderTemplate` ${maybeRenderHead()}<nav class="top-nav"> <a href="/" class="btn-back-fixed"> <i class="fa-solid fa-chevron-left"></i>
Voltar para Início
</a> </nav> <link rel="stylesheet" href="/styles/variables.css"> <link rel="stylesheet" href="/styles/global.css"> <link rel="stylesheet" href="/styles/layout.css"> <link rel="stylesheet" href="/styles/pages/produtos/favoritos.css"> <div class="hero-section"> <h1>Meus Favoritos <i class="fa-solid fa-heart" style="color: #fff;"></i></h1> <p>A lista dos seus produtos mais desejados.</p> </div> <div class="main-content"> <div id="fav-grid" class="fav-grid"> <div class="empty-state"> <i class="fas fa-circle-notch fa-spin loading-spinner"></i> <p style="margin-top: 15px;">Carregando seus achadinhos...</p> </div> </div> </div> ${renderComponent($$result2, "Favoritos", $$Favoritos, {})} ${renderComponent($$result2, "Global", $$Global, {})} ` })}`;
}, "C:/Users/joaos/OneDrive/Documents/baudepromo/radiant-radiation/src/pages/produtos/favoritos/index.astro", void 0);

const $$file = "C:/Users/joaos/OneDrive/Documents/baudepromo/radiant-radiation/src/pages/produtos/favoritos/index.astro";
const $$url = "/produtos/favoritos";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
    __proto__: null,
    default: $$Index,
    file: $$file,
    url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
